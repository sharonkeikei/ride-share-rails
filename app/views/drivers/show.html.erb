<h1>Driver #<%=@driver.id%> info<h1>
<br>
<h3> Driver Name: <%= @driver.name%> </h3>
<p>Vin #<%=@driver.vin %></p>
<p>Avilability: <%= (@driver.available) ? 'Yes' : 'No!'%></p>
<%= link_to 'Delete Driver', driver_path(@driver.id), class: 'btn btn-outline-danger btn-sm', method: :delete, data: { confirm: 'Are you sure?'} %>
<%= link_to 'Toggle available', toggle_available_path(@driver.id), class: 'btn btn-outline-secondary btn-sm', method: :patch %>